# Восстановлено в рамках аудита (2026-07-21): в git-репозитории этого файла не было,
# хотя код на него ссылается (см. README.md "pip install -r requirements.txt").
# Версии ниже подобраны по фактическому импорту в *.py и текущим стабильным релизам.
# Если на сервере используются другие версии — замените здесь и закоммитьте.

# --- Асинхронный HTTP / вебсокеты ---
aiohttp==3.9.3
websockets==12.0
aiofiles==23.2.1

# --- Telegram-бот (UI) ---
aiogram==3.4.1

# --- Работа с BSC / EVM (Web3) ---
web3==6.15.1
eth-account==0.11.0
eth-abi==5.0.1
python-socks==2.4.3

# --- MEXC / биржевой клиент ---
ccxt==4.2.79
