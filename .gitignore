# Секреты и локальная конфигурация — НИКОГДА не коммитить
config.py
CLAUDE.md
.env

# База данных бота (приватные ключи, API-ключи MEXC, балансы)
*.db
*.db-journal
*.sqlite
*.sqlite3

# ABI конкретных токенов пользователя (не секрет, но специфично для инстанса —
# при необходимости можно убрать из .gitignore и закоммитить осознанно)
pair_abi/*.json

# Виртуальное окружение / кэш Python
venv/
.venv/
__pycache__/
*.pyc

# Логи
*.log

# IDE
.idea/
.vscode/
